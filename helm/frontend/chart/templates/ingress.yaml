apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{.Values.component}}-ingress
spec:
  defaultBackend:
    service:
      name: {{.Values.component}}-service
      port:
        number: 80
